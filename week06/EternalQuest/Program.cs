using System;

class Program
{
    static void Main(string[] args)
    {
        GoalManager goalManager = new GoalManager(); // starts the goalmanager 
        goalManager.Start();// starts the manager
    }
}